---
trigger: always_on
---

Going forward, every task must follow these rules strictly:

Only do exactly what the task description says — nothing more
Do not create files that were not explicitly requested
Do not modify files that were not explicitly mentioned in the task
If you notice something that could be improved outside the task scope, mention it in your summary but do not implement it
End every task with the exact validation commands specified and share their output
